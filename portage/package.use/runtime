# Static busybox → /bin/sh in cli-build / fpm-build only (not production cli/fpm)
# REQUIRED_USE: pam? (!static) — PAM and static are mutually exclusive
# static busybox pulls in virtual/libcrypt[static-libs]
sys-apps/busybox static -pam
virtual/libcrypt static-libs
sys-libs/libxcrypt static-libs
