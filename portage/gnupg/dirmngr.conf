# Gentoo snapshot / portage sync in Docker builders.
# Fallback when gemato or gpg need an explicit keyserver (host network builds).
keyserver hkps://keys.gentoo.org
honor-keyserver-url-used
disable-ipv6
